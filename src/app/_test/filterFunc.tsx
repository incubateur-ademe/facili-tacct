//   const moule = await PrismaPostgres.clc_paris.findMany().then(result => { console.log(result) }).catch((e) => console.log(e));
//   ;
//  //{where: { pk: 10}}
// import { Get_Prisma } from "./prismafunc";

// export async function Filter_db() {
  
//   const data_filtered = (await Get_Prisma()).props.value.find((el) => el.pk === 300)

//   return data_filtered;
// }
